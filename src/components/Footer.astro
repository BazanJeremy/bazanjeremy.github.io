---
import LangToggle from './LangToggle.astro';
import { useTranslations, type Locale, type AltPaths } from '../i18n/utils';

interface Props {
  lang: Locale;
  altPaths: AltPaths;
}

const { lang, altPaths } = Astro.props;
const t = useTranslations(lang);
---

<footer class="border-t border-line">
  <div
    class="wrap flex flex-col gap-4 py-8 sm:flex-row sm:items-center sm:justify-between"
  >
    <p class="font-mono text-xs text-muted">{t.footer.copyright}</p>
    <LangToggle lang={lang} altPaths={altPaths} />
  </div>
</footer>
